#include "entities.h"
#include <iostream>
#include <fstream>

namespace moseldata {
	

	using namespace entities;
	using namespace std;

	dataContent parseMoselData(char * filename) {
		ifstream inFile(filename);

		return dataContent();
	}
}